---
// Distinct 404 metadata (the vinext build reused the homepage title — logged
// during the migration and fixed here). Description reuses the page's own
// approved copy; no canonical on an error response.
import Layout from "../layouts/Layout.astro";
import NotFound from "../components/not-found";
---

<Layout
  title="Page not found | Onduu"
  description="This page is not part of the current system. The link may be old, gated or waiting for approval. Start with the Readiness Score or return home."
>
  <NotFound />
</Layout>
