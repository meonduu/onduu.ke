---
// Domain search across the .co.ke/.ke pair, with registration routed to the
// approved HOSTAFRICA destination (UTM attribution only; no commission).
import Layout from "../layouts/Layout.astro";
import { DomainsPage } from "../components/domains-page";
import { DomainsForm } from "../components/domains-form";
---

<Layout
  title="Kenyan Domain Search: Registrar, Lock and Expiry | Onduu"
  description="Search any .ke or .co.ke domain alongside its twin. See the registrar, transfer lock and expiry from public records — free, no signup, no account."
  canonical="https://onduu.ke/kedomains"
>
  <DomainsPage>
    <DomainsForm client:load />
  </DomainsPage>
</Layout>
